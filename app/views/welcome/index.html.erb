<% if !current_user %>
  <h2 class="page-header" >Hey there, friend! </h2>

  <p>If this is your first time here, hey/how ya doing?</p>
  <p>Either way, <%= link_to "Sign Up", new_user_registration_path, :class => "link" %> or <%= link_to "Log In", new_user_session_path, :class => "link" %></li> to get going. </p>


<% else %>
  <h2 class="page-header" >Hey there! What should we do today to feel accomplished?</h2>
  <h2>Your Accomplists</h2>

  <table class="accomplist-table">
    <% starting_date = Date.new(2018, 1, 1) %>
    <% week = 0 %>
    <% 7.times do %>
      <% date = starting_date + week %>
      <tr>
        <% 52.times do %>

          <!-- temporary solution for links instead of trying to make empty anchor clickable. don't judge me. -->
          <% list = find_list_by_user_and_day(all_lists_for_user, date) %>
          <% link = "window.location = '/users/#{current_user.id}/lists/#{list.id}'" if list %>
          <% cursor = 'style=cursor:pointer;' if list %>

          <td
            onClick="<%= link %>"
            class="accomplist-table__td <%= table_cell_colour(list) %>" data-date="<%= date.to_s %>"
            title="<%= date.to_s %>"
            <%= cursor %>
          >
          </td>
        <% date = date + 7  %>
        <% end %>
      </tr>
      <% week = week + 1 %>
    <% end %>
  </table>

  <p><%= link_to "Start a new list for today", user_list_path(current_user, current_user.todays_list), :class => "link" %></p>

  <h2>iterate through days so far</h2>

<% end %>
