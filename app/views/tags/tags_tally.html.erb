<h1 class="page-header">Your Most Used Tags</h1>
<ul class="tags-list">
  <% current_user.user_tags_sorted.each do |tag| %>
  <li>"<%= tag[0] %>" was used in <%= tag[1] %> posts</li>
  <% end %>
</ul>
